## Temporary implementation plans

This directory holds temporary implementation plans. These plans generally only last for the
duration of a pull request, and should be deleted before the PR is merged into `main`. Although
sometimes it makes sense for an implementation plan to live for multiple PRs if the plan is large
enough to justify a phased implementation, one phase per PR.

