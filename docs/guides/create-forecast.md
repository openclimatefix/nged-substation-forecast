# Create a Forecast

To create a new forecast, follow these steps:

1.  Ensure your environment is set up with `uv sync`.
2.  Run the Dagster UI: `uv run dg dev`.
3.  Trigger the forecast pipeline for the desired partition.
4.  Monitor the progress in the Dagster UI.
5.  The forecast will be saved to the configured storage location.
