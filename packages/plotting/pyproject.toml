[project]
name = "plotting"
version = "0.2.0"
description = "OCF brand Altair theme"
requires-python = ">=3.14"
dependencies = [
    "altair>=6.0.0",
    "polars>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
