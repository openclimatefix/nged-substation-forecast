# Commits listed here are skipped by `git blame` (GitHub does this automatically; a local
# `git blame` needs `git config blame.ignoreRevsFile .git-blame-ignore-revs`, run once per clone).

# Hard-wrap docs at 100 characters (#690) — rewraps almost every line in docs/, the READMEs,
# CLAUDE.md and the skill files, which would otherwise attribute every line it touches to this
# commit instead of whoever wrote the words.
08c23ff4c89d52af32a5df256062d1638c0398ae
